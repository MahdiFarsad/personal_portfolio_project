export const metadata = {
  title: "Blog",
  description: "Notes on machine learning, data science, and software projects.",
};

export default function BlogLayout({ children }) {
  return children;
}
