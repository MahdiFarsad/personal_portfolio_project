export const metadata = {
  title: "Projects",
  description: "Machine learning, data science, and full-stack projects.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
